{
  "name": "kawkabat-alarqam-alsehriya",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start:server": "cd server && npm start",
    "start:full": "concurrently \"npm run start:server\" \"npm run dev\"",
    "update-data": "python scripts/data_fetcher.py",
    "setup": "npm install && cd server && npm install && cd .. && pip install -r requirements.txt"
  },
  "dependencies": {
    "html2canvas": "^1.4.1",
    "jspdf": "^3.0.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-zoom-pan-pinch": "^3.7.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^6.3.5",
    "concurrently": "^8.2.2"
  }
}
